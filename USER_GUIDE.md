# 音樂播放器使用指南 (Music Player User Guide)

## 功能概覽

本音樂播放器實現了以下所有需求功能：

### ✅ 1. 播放清單建立
- 使用 QMediaPlaylist 管理多首歌曲
- 支援增加、刪除歌曲功能
- 可調整歌曲播放順序（上移/下移按鈕）
- 用戶可自由調整歌曲順序或刪除不需要的歌曲

### ✅ 2. 上/下首歌曲
- 利用 QMediaPlayer API 實現「上一首」與「下一首」功能
- 提供直觀的操作按鈕 (⏮ 上一首、⏭ 下一首)
- 用戶可輕鬆切換歌曲

### ✅ 3. 顯示播放速度
- 透過 QSlider 控制播放進度
- 使用 player.positionChanged 即時更新播放位置
- 顯示已播放時間和總時長
- 時間格式為 mm:ss (例如: 02:35)
- 用戶可拖動滑桿跳轉到任意播放位置

### ✅ 4. 歌曲封面展示
- 透過 mutagen 庫讀取 MP3 文件的嵌入封面
- 封面顯示在 QLabel 上
- 切換歌曲時自動更新封面
- 支援 MP3 (ID3) 和 FLAC 格式的封面提取

### ✅ 5. 播放器控制界面設計
- 提供完整的播放器控制按鈕：
  - ▶ 播放
  - ⏸ 暫停
  - ⏹ 停止
  - ⏮ 上一首
  - ⏭ 下一首
- 音量控制 (QSlider，0-100%)
- 介面簡潔直觀

### ✅ 6. 播放清單管理功能
- 支援多個播放清單的建立
- 可儲存播放清單到 JSON 檔案
- 可從 JSON 檔案載入播放清單
- 可隨時切換不同播放清單
- 方便管理不同的音樂分類

### ✅ 7. 音樂檔案格式支援
支援多種音頻格式：
- MP3
- WAV
- FLAC
- AAC
- M4A
- OGG

用戶可透過文件選擇器選擇本地音樂檔案進行播放

### ✅ 8. 字幕顯示及點擊字幕跳轉音樂
- 字幕顯示使用 QListWidget
- 每一行代表一句字幕
- 支援 LRC 格式字幕檔案
- 字幕會依照音樂播放進度自動高亮顯示當前內容
- 自動滾動到當前播放的字幕位置
- 用戶點擊任一行字幕後，音樂立即跳轉到該字幕對應的時間位置
- 方便反覆聆聽指定段落
- 自動載入與歌曲同名的 .lrc 檔案

## 詳細功能說明

### 播放清單操作

#### 添加歌曲
1. 方法一：點擊「➕ 添加歌曲」按鈕
2. 方法二：使用選單「檔案」→「添加音樂檔案」
3. 選擇一個或多個音頻檔案
4. 歌曲將自動添加到當前播放清單

#### 刪除歌曲
1. 在播放清單中選擇要刪除的歌曲
2. 點擊「➖ 刪除歌曲」按鈕

#### 調整順序
1. 在播放清單中選擇歌曲
2. 點擊「⬆ 上移」向上移動
3. 點擊「⬇ 下移」向下移動

#### 播放指定歌曲
- 雙擊播放清單中的任意歌曲即可播放

### 播放控制

#### 基本控制
- **播放**: 點擊「▶ 播放」開始播放
- **暫停**: 點擊「⏸ 暫停」暫停播放
- **停止**: 點擊「⏹ 停止」停止播放
- **上一首**: 點擊「⏮ 上一首」播放前一首歌曲
- **下一首**: 點擊「⏭ 下一首」播放下一首歌曲

#### 進度控制
- 拖動進度滑桿可跳轉到指定播放位置
- 當前時間和總時長顯示在滑桿兩側 (格式: mm:ss)

#### 音量控制
- 拖動音量滑桿調整音量
- 音量範圍: 0-100%
- 當前音量值顯示在滑桿右側

### 封面顯示
- 封面自動從音樂檔案中提取
- 支援 MP3 (ID3 標籤) 和 FLAC 格式
- 切換歌曲時自動更新
- 如果歌曲沒有封面，顯示「無封面」

### 字幕功能

#### LRC 字幕格式
```
[00:00.00]第一句歌詞
[00:05.50]第二句歌詞
[00:10.20]第三句歌詞
```

時間格式: [分:秒.毫秒]

#### 載入字幕
1. **自動載入**: 如果歌曲檔案旁有同名的 .lrc 檔案，會自動載入
   - 例如: song.mp3 → song.lrc
2. **手動載入**: 點擊「📝 載入字幕」按鈕選擇字幕檔案

#### 字幕同步
- 當前播放的字幕行會以黃色高亮顯示
- 字幕會自動滾動到當前位置

#### 點擊跳轉
- 點擊任意字幕行
- 音樂立即跳轉到該字幕的時間點
- 方便重複聽特定段落

### 多播放清單管理

#### 新建播放清單
1. 選單「播放清單」→「新建播放清單」
2. 輸入播放清單名稱
3. 新的播放清單建立完成

#### 切換播放清單
1. 選單「播放清單」→「切換播放清單」
2. 從列表中選擇要切換的播放清單
3. 界面會載入該播放清單的所有歌曲

#### 保存播放清單
1. 選單「播放清單」→「保存播放清單」
2. 選擇保存位置和檔案名
3. 所有播放清單保存為 JSON 格式

#### 載入播放清單
1. 選單「播放清單」→「載入播放清單」
2. 選擇之前保存的 JSON 檔案
3. 所有播放清單將被載入

## 技術實現細節

### 使用的 Qt 組件
- **QMediaPlayer**: 媒體播放控制
- **QMediaPlaylist**: 播放清單管理
- **QSlider**: 進度條和音量控制
- **QLabel**: 封面和資訊顯示
- **QListWidget**: 播放清單和字幕顯示
- **QMenuBar/QMenu**: 選單系統

### 關鍵功能實現

#### 1. 播放清單同步
- 維護三個同步的資料結構：
  - QMediaPlaylist (Qt 媒體播放清單)
  - Python list (檔案路徑列表)
  - QListWidget (界面顯示)
- 任何操作都同步更新三個結構

#### 2. 進度更新
```python
player.positionChanged.connect(update_position)
```
- 使用信號/槽機制即時更新進度
- 格式化時間顯示 (毫秒 → mm:ss)

#### 3. 封面提取
```python
# 使用 mutagen 讀取
audio_file = MutagenFile(file_path)
# 從 ID3 標籤或 FLAC 提取封面
```

#### 4. 字幕同步
- 解析 LRC 檔案，提取時間戳和歌詞
- 使用字典存儲: {timestamp: lyrics_text}
- positionChanged 信號觸發高亮更新
- 找到小於等於當前時間的最後一個字幕行

#### 5. 字幕跳轉
```python
lyrics_widget.itemClicked.connect(jump_to_lyrics_time)
# 從 item 的 UserRole 資料中讀取時間戳
player.setPosition(timestamp)
```

## 程式架構

```
MusicPlayer (QMainWindow)
├── QMediaPlayer (player)
├── QMediaPlaylist (playlist)
├── UI Components
│   ├── QLabel (cover_label) - 封面顯示
│   ├── QLabel (song_info_label) - 歌曲資訊
│   ├── QSlider (progress_slider) - 進度條
│   ├── QSlider (volume_slider) - 音量控制
│   ├── QListWidget (playlist_widget) - 播放清單
│   └── QListWidget (lyrics_widget) - 字幕顯示
├── Control Buttons
│   ├── play_button
│   ├── pause_button
│   ├── stop_button
│   ├── prev_button
│   └── next_button
└── Data Structures
    ├── song_list (檔案路徑)
    ├── lyrics_data (字幕資料)
    └── playlists (多播放清單)
```

## 測試建議

### 功能測試清單

1. **播放清單管理**
   - [ ] 添加單個音樂檔案
   - [ ] 添加多個音樂檔案
   - [ ] 刪除歌曲
   - [ ] 上移歌曲
   - [ ] 下移歌曲
   - [ ] 雙擊播放

2. **播放控制**
   - [ ] 播放按鈕
   - [ ] 暫停按鈕
   - [ ] 停止按鈕
   - [ ] 上一首
   - [ ] 下一首

3. **進度和音量**
   - [ ] 拖動進度條
   - [ ] 時間顯示正確
   - [ ] 調整音量
   - [ ] 音量百分比顯示

4. **封面顯示**
   - [ ] 播放有封面的 MP3
   - [ ] 播放有封面的 FLAC
   - [ ] 播放無封面的音樂

5. **字幕功能**
   - [ ] 自動載入同名 LRC
   - [ ] 手動載入 LRC
   - [ ] 字幕高亮正確
   - [ ] 字幕自動滾動
   - [ ] 點擊字幕跳轉

6. **多播放清單**
   - [ ] 新建播放清單
   - [ ] 切換播放清單
   - [ ] 保存播放清單
   - [ ] 載入播放清單

7. **多格式支援**
   - [ ] MP3 檔案
   - [ ] WAV 檔案
   - [ ] FLAC 檔案
   - [ ] AAC 檔案

## 常見問題

### Q: 無法顯示封面？
A: 確保音樂檔案包含嵌入式封面圖片。可使用音樂標籤編輯器（如 Mp3tag）添加封面。

### Q: 字幕不同步？
A: 檢查 LRC 檔案的時間標記是否正確，格式應為 [mm:ss.xx]。

### Q: 無法播放某些音頻格式？
A: 確保系統安裝了相應的編解碼器。PyQt5 依賴系統的多媒體框架。

### Q: 字幕檔案亂碼？
A: 確保 LRC 檔案使用 UTF-8 編碼保存。

### Q: 播放清單保存在哪裡？
A: 播放清單保存為 JSON 格式，由用戶選擇保存位置。

## 未來改進方向

1. 添加播放模式（順序播放、隨機播放、單曲循環）
2. 支援更多字幕格式（SRT、ASS）
3. 等化器功能
4. 播放歷史記錄
5. 搜尋和過濾功能
6. 快捷鍵支援
7. 主題和樣式自定義
8. 播放統計資訊

## 總結

本音樂播放器完整實現了所有需求功能：
- ✅ 播放清單建立與管理
- ✅ 上下首歌曲切換
- ✅ 播放進度顯示與控制
- ✅ 歌曲封面展示
- ✅ 完整的播放器控制界面
- ✅ 多播放清單管理
- ✅ 多種音樂格式支援
- ✅ 字幕顯示與點擊跳轉

所有功能都按照需求使用 PyQt5 和相關庫實現，代碼結構清晰，易於維護和擴展。
